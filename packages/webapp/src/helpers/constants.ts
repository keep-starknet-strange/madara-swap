// In percent -> 6 percent by default to avoid too many tx failed as StarkNet is slow
export const DEFAULT_SLIPPAGE = 6;
// export const DEFAULT_SLIPPAGE = 0.5;

// Slippage severity breakpoint in percent
export const SLIPPAGE_SEVERITY = [0.1, 10, 50];
// export const SLIPPAGE_SEVERITY = [0.1, 5, 50];
